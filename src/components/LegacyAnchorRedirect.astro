---
interface Props {
  description: string;
  target: string;
  title: string;
}

const { description, target, title } = Astro.props;
const site = Astro.site ?? new URL('https://solutenerji.com');
const canonicalHome = new URL('/', site).href;
---
<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{title}</title>
  <meta name="description" content={description} />
  <meta name="robots" content="noindex,follow" />
  <link rel="canonical" href={canonicalHome} />
  <meta http-equiv="refresh" content={`0;url=${target}`} />
</head>
<body>
  <p>
    Yönlendiriliyorsunuz.
    <a href={target}>Devam etmek için tıklayın.</a>
  </p>
</body>
</html>
